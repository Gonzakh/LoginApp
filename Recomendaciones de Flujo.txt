
-- Flujo para relación de Console UI - Aplication:

// Poner un DTO aqui  es un error, ya que no representa al user del Dominio, sino el Input del usuario. 
// DTOs son para la capa de Application, no para la UI.

Console UI

-> lee input
-> llama RegisterUser
-> RegisterUser valida/normaliza
-> hashea password usando IPasswordHasher
-> crea User
-> guarda User usando IUserRepository
-> Console UI muestra mensaje

UI input

-> RegisterUserRequest
-> RegisterUser.Execute(request)
-> RegisterUserResult
-> UI output

-- La UI solo recolecta datos. No valida email. No hashea. No crea User.


--Anotaciones:

//Result.cs sirve si querés evitar excepciones para errores esperados, como email duplicado.
//Excepción mejor para cosas inesperadas.
